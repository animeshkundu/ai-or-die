# Development files
*.tgz
package/
node_modules/
.git/
.gitignore
*.log
.DS_Store

# Test files
test/
tests/
*.test.js
*.spec.js
*_test.go
test-results/
e2e/
poc/

# Build artifacts
dist/
build/
*.tmp

# CI & tooling
.github/
.claude/
.cursor/
.prompts/

# Documentation (README.md and LICENSE always included by npm)
docs/
# Local working notes / research. NOT covered by the `docs/` rule above — the
# leading dot makes it a distinct path, and it was shipping to every installer
# (10 files of local research plus scratch HTML). Contents are developer-local
# and sometimes private, so this is a leak, not just bloat.
.docs/
# Agent scratch (opencode plans). Same leak class as .docs/ above: local
# drafts that must never ship (the canonical ADR/spec live in docs/).
.opencode/
site/
AGENTS.md
CLAUDE.md
CHANGELOG.md
CONTRIBUTING.md

# Build & release scripts
scripts/
