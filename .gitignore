node_modules/
.DS_Store
*.log
.env
.env.local
.env.production
.env.development
*.pem
*.key
*.crt
.idea/
.vscode/
*.swp
*.swo
*~
.claude/
test-*.js

# Factory operational state (ephemeral, tracked on factory branch only)
docs/factory/state/

# Test runtime artifacts — Playwright trace/result/screenshot output and
# the generic-drop pipeline's per-session attachment dir created when the
# upload endpoint runs against a fixture.
test-results/
e2e/.claude-attachments/
# e2e artifact-panel test fixtures (created under repo root for validateArtifactPath)
.tmp-e2e-artifact-*/
